# UI Layer

`chatbox.html` is currently served from the project root `chatagent/chatbox.html`.

The HTTP app factory already checks this `ui/` folder first and falls back to the legacy path.
You can move the file here directly in a later cleanup without changing route code.

